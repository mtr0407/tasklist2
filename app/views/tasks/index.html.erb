<h1>タスクリスト</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %>:<%= task.title %> &gt;<%= task.content %>&gt;<%= task.status %></li>
  <% end %>
</ul>

<%= link_to 'タスクの新規作成', new_task_path %>
